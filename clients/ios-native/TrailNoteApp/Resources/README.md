# TrailNote iOS Resources

Place iOS resources here when the Xcode project is generated.

- `Assets.xcassets`
- `Localizable.strings`
- launch and app icon resources

Keep token-driven color and typography values in `DesignSystem/` as the source of truth.
